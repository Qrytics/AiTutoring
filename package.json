{
	"name": "aitutoring",
	"private": true,
	"version": "0.0.1",
	"workspaces": [
		"apps/*",
		"packages/*"
	],
	"scripts": {
		"dev": "npm run dev --workspace=apps/web",
		"build": "npm run build --workspace=apps/web",
		"preview": "npm run preview --workspace=apps/web",
		"check": "npm run check --workspace=apps/web"
	},
	"overrides": {
		"cookie": "^0.7.0"
	}
}
