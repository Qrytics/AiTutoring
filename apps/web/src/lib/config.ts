/**
 * Central configuration constants.
 * Kept in a shared lib file so there is a single source of truth for values
 * that appear in both client and server code (e.g. the base path).
 *
 * NOTE: `kit.paths.base` in svelte.config.js must match BASE_PATH below.
 */
export const BASE_PATH = '/tutoring';
