{
	"buildCommand": "npm run build --workspace=apps/web && rm -rf .vercel/output && cp -R apps/web/.vercel/output .vercel/output",
	"outputDirectory": ".vercel/output",
	"installCommand": "npm install",
	"framework": null
}
